import { component$ } from "@builder.io/qwik";

export default component$(() => {
  return (
    <>
      <h2 class="text-lg font-bold text-pink-300">Dashboard Administrativo</h2>
      <p>Este es el dashboard para configurar las opciones administrativas</p>
    </>
  );
});
